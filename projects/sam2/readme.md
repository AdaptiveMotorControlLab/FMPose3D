## Env



# test_video.py

for frame_dir:

if frame_dir is empty:
    get the video_name from video_path
    set the frame_dir = folder(video_path)+video_name


make a function to load video and save images to the target folder (frame_dir);

propagate through the video
plot each image with the mask,
save images to target_frame_dir, with the same name as the original image